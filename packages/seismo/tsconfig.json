{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No DOM and no Node: the library runs in the browser, the Worker and Node alike, so it may
    // use none of their globals. The compiler is what enforces that.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src", "test"]
}
